name: CI

on:
  pull_request:
    branches: [main, codex]

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Temporary placeholder
        run: echo "Restoring normal CI"
